apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k5s-dev-staging
spec:
  secretName: k5s-dev-staging-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  commonName: k5s.dev
  dnsNames:
    - k5s.dev
    - '*.k5s.dev'
