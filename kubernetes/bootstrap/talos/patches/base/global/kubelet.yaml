machine:
  kubelet:
    # extraArgs:
    #   rotate-server-certificates: true
    nodeIP:
      validSubnets:
        - 10.0.8.0/24
    extraMounts:
      - destination: /var/mnt/longhorn
        type: bind
        source: /var/mnt/longhorn
        options:
          - bind
          - rshared
          - rw
