# Trash
.DS_Store
Thumbs.db
# Cursor agent plans (local only)
.cursor/plans/
# k8s / Omni local credentials
kubeconfig
kubeconfig-headlamp-desktop
talosconfig
omniconfig
1password-token
HWIDs.md
# SecureBoot install ISOs (large; regenerate via omnictl media download)
secureboot-media/
#temp
/temp
# Omni bootstrap: always render fresh via `mise run manifests-render`
/kubernetes/bootstrap/talos/manifests/*.yaml
